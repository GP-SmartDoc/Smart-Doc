#inside frontend folder run only once
npm install
npm install mermaid
npm install --save-dev concurrently wait-on

#in frontend folder package.json in scripts "backend"
change path of my env to your env 

#to run inside frontend
npm run dev

##to run visualization type in cmd while you at SmartDoc " python -m tests.visualization_test "
